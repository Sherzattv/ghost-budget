# Changelog

Все изменения проекта Ghost Budget документируются здесь.

## [2026-01-27] - Исправление подключения Supabase

### Fixed
- 🔧 **SUPABASE_URL** — был URL дашборда, исправлен на API URL
- 🔧 **Генерация UUID** — добавлена при создании профиля (БД не имела default)
- 🔧 **RLS политики** — добавлены для `anon` роли:
  - `profiles`: SELECT/INSERT/UPDATE по telegram_id
  - `accounts`: SELECT/INSERT/UPDATE
  - `categories`: SELECT/INSERT
  - `transactions`: SELECT/INSERT/DELETE
- 🔧 **FK constraint** — удалён `profiles_id_fkey` на `auth.users` (не нужен для Telegram бота)

### Improved
- 📝 Добавлено детальное логирование в `get_user_by_telegram_id`

---

## [2026-01-27] - Рефакторинг архитектуры бота (ранее)

### Added
- ✨ **Onboarding flow** — интерактивный onboarding для новых пользователей
- ✨ **CallbackData** — замена JSON на типизированные CallbackData объекты
- ✨ **Middleware** — DbSessionMiddleware для инъекции Supabase клиента
- ✨ **Bot commands** — команды в меню Telegram (/start, /balance, /stats, /help)

### Changed
- 🔄 Обработчик `/start` теперь показывает разные сообщения для новых и существующих пользователей
